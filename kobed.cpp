#include "kobed.h"

namespace PXL2023
{
int KobeD::willYouRaise( unsigned int totalBet ){





















    /*
        PokerRank myrank = getMyHandRank(player->hand);
        if (myrank.rank >= FULL_HOUSE)
        {
            return 10000;
        }

        if (myrank.rank <= TWO_PAIR)
        {
            return -1;
        }
        else
        {
            if(numberOfCallersOnStart(game, player)>= 3){
                if (myrank.rank >= THREE_OF_A_KIND)
        {
            return 10000;
        }
            }
            return 0;
        }
    */





    return 0;

}

const char * KobeD::getName( void ) const
{
    return "Kobeke";
}
}
